---
name: yiqi-yice-generator
description: Use when the user needs to generate "一企一册" practical semester learning manuals for multiple enterprises. Triggered by keywords like 一企一册, 实践学期, 学习手册, 教学手册, 实习手册, 专业实践. The user typically provides enterprise names, practice periods (weeks), majors, and job directions.
---

# 一企一册生成器

## 概述

一键生成"一企一册"实践学期学习手册 Word 文档。  
根据企业信息、专业方向、实践周期等信息，自动生成包含通用部分（企业概况/安全规范/沟通协作/职业守则）、岗前培训、专业部分（岗位职责/流程/进度/考核）的完整 docx 文档。

**核心逻辑**：Python + zipfile 直接操作 docx XML，从模板复制17个预置表格，替换 document.xml。

## 前置准备

1. 准备好模板文件路径和表格 XML 目录
2. 确认企业信息（名称、成立年份、注册资金、地点、主营业务、组织架构）
3. 确认专业方向/岗位方向
4. 确认实践周期（周数）
5. 确认编写人姓名

## 工作流程

### Step 1：收集信息

从用户处获取以下输入：
- **企业列表**：企业名称、基本信息
- **专业**：如电子商务、计算机等
- **岗位方向**：如电商开发、内容制作、设备运维、AI短视频等
- **周期**：8周或4周
- **编写人**：默认 xxx
- **学院名称**：如 xxx学院
- **模板路径**：原始 docx 模板路径
- **输出目录**：最终文件存放位置

### Step 2：分析岗位→考核要素映射

根据岗位方向，确定score2(6项)和score3(4项)的考核要素及考核内容描述。  
参考 `word-template-reference.md` 第8.3节的映射表。

### Step 3：生成脚本

创建一个生成脚本，核心逻辑：

```python
# 1. 加载模板表格
# 2. 定义字体/段落/表格函数
# 3. 构建文档内容（封面→目录→通用部分→岗前培训→专业部分→周记→考核）
# 4. 替换考核表内容（文字直接替换）
# 5. 打包输出 docx
```

关键注意事项：
- **目录**必须手写带页码，不要用TOC域代码
- **周记**每页分开，加 `cantSplit` 防跨页
- **考核表**不加 cantSplit，允许自然分页
- **字体**：正文仿宋_GB2312 12pt，标题黑体加粗
- **分页符**：封面后/目录后/每部分前/每个周记前/每个考核表前
- **无底色**：所有自定义表格无shading

### Step 4：生成并交付

输出文件命名规则：`企业名_专业实践学期学习手册.docx`

### Step 5：后续调整

用户可能需要手动校对后反馈，主要调整点：
- 目录页码是否正确
- 考核内容是否匹配岗位
- 周记周数是否正确
- 企业信息是否准确

## 调用方法

生成脚本可以直接作为 Python 文件运行：

```bash
python gen_企业名.py
```

每次生成一个企业。需要先读取用户的全部输入。

## 快速参考

### 文档内容结构

```
封面 → 目录 → 第一部分(通用) → 第二部分(岗前培训) → 第三部分(专业)
├── 一、企业概况（基本情况/组织架构/主营业务/服务内容）
├── 二、安全规范与应急处理（4条）
├── 三、沟通协作基础（3条+记录表）
├── 四、职业守则（4条+确认表）
├── 岗前培训记录表（8项）
├── 一、岗位职责与任务（任务书+论文表）
├── 二、岗位工作流程与标准（流程表)
├── 三、实践任务与进度安排（教学安排表+周记×N周）
├── 四、考核评价标准（评价表+成绩核定表×3+KPI表）
```

### 页面设置

```
A4: 11906×16838 DXA
页边距: 上下1440(2cm) 左右1440(2cm)
内容宽度: 9026 DXA
```

### 字体对照

| 元素 | 字体 | 字号(DXA) | 倍率 |
|------|------|-----------|------|
| 封面标题 | 黑体加粗 | 44 | 22pt |
| 大标题 | 黑体加粗 | 36 | 18pt |
| 中标题 | 黑体加粗 | 32 | 16pt |
| 小标题 | 黑体加粗 | 24 | 12pt |
| 正文 | 仿宋_GB2312 | 24 | 12pt |
| 表格 | 仿宋_GB2312 | 24 | 12pt |

## 常见错误

1. **目录TOC域报错** → 不要用TOC域，手工写带页码目录
2. **表格跨页断裂** → 周记表加 cantSplit，考核表不加
3. **考核内容未匹配** → 可能是跨<w:t>标签，用正则`<w:t>X</w:t></w:r><w:r>.*?<w:t>Y</w:t>`替换
4. **字体不统** → 确保所有段落指定 w:ascii/w:hAnsi/w:eastAsia 为统一字体
5. **分页符过多/过少** → 只有表前加分页符，通用部分4节连续排版
6. **底纹出现** → 检查XML中w:shd标签，自己写的表格不要加shading

## 参考文件

- `word-template-reference.md` — 完整代码模式和表格索引
